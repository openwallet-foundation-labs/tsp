# Docs

This directory contains the appendix-facing test-vector documentation.

Use:

- `spec/`
  for vector definitions, complete-case descriptions, manifests, and
  case-level outcomes

`spec/` is the canonical documentation surface for `tsp_test_vectors`.
